<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Google fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Sacramento&display=swap" rel="stylesheet">

    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com/" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Raleway:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
    <style>
        body {
            background-color: #FFFF; /* Light Gray */
        }
        h1 { /* settings */
            font-family: 'Sacramento', serif; /* Applying Sacramento font */
            font-weight: 400;
            font-style: normal;
            font-size: 70px;
            margin-top: 70px;
        }
        .d-grid .btn {
          font-size: 23px;
          margin-top: 10px;
          margin-bottom: 24px; 
        }
        button{
          font-family: "Raleway", serif;
          font-optical-sizing: auto;
          font-weight: 300;
        }
        .center-form {
          display: flex;
          justify-content: center; 
          align-items: center; 
          }

        .btn1{
          padding: 10px 20px; 
          font-size: 16px; 
          border-radius: 5px; 
          outline-offset: 4px; 
        }
    </style>
  </head>
  <div>
    <h1 class="text-center mb-4">Settings</h1>
    <div class="d-grid">
                  <button type="submit" class="btn btn-dark">Account Information</button>
                </div>
    <div class="d-grid">
                  <button type="submit" class="btn btn-dark">Edit Inventory</button>
                </div>
    <div class="d-grid">
                  <button type="submit" class="btn btn-dark">Appearance</button>
                </div>
    <div class="d-grid">
                   <button type="submit" class="btn btn-dark">Language</button>
                </div>
    <div class="d-grid">
                  <button type="submit" class="btn btn-dark">Terms of Use</button>
                </div>
    <div class="d-grid">
                </div>
    <div class="d-grid">
                    <button type="submit" class="btn btn-dark">Help</button>
                </div>
    <div class="d-grid">
      <form action="/secret" method="POST" class="center-form">
        <button class="btn1">Go Back</button>
      </form>
                </div>

